{
  "name": "common-types",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "description": "Just shared types...",
  "private": true,
  "scripts": {
    "build": "rm -r dist && npx tsc -p ."
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
